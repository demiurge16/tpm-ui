export enum FieldType {
  UNIQUE_TOKEN = "unique_token",
  STRING = "string",
  NUMBER = "number",
  DATE = "date",
  DATETIME = "datetime",
  BOOLEAN = "boolean",
  SELECT = "select",
  MULTISELECT = "multi_select"
}
